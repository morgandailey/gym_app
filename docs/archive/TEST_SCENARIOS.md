# Test Scenarios

## 這份文件的目的

這份文件用來把目前專案的主流程與高風險區，轉成可執行的測試場景。

它要回答的問題是：

> 如果現在要驗證這個系統有沒有正常運作，應該測哪些路徑？先測什麼？怎樣才算通過？

## 這份文件適合什麼時候看

當你要做下面這些事情時，應該先看這份：

- 手動測試目前功能
- 修完 bug 後驗證有沒有壞別的地方
- 重構前後做比對
- 規劃未來測試策略

## 這份文件和其他文件的關係

- 上層入口： [README.md](./README.md)
- 現況總覽： [CURRENT_STATE.md](./CURRENT_STATE.md)
- 問題盤點： [KNOWN_ISSUES.md](./KNOWN_ISSUES.md)
- 使用流程： [USER_FLOW.md](./USER_FLOW.md)
- 產品範圍： [PRODUCT_SCOPE.md](./PRODUCT_SCOPE.md)
- 資料模型： [DATA_MODEL.md](./DATA_MODEL.md)
- 方向規劃： [APP_TRANSITION_PLAN.md](./APP_TRANSITION_PLAN.md)
- 這份文件角色：負責「驗證場景」

---

## 一句話總結

目前這個專案最需要的不是完整自動化測試，而是先有一份清楚的手動驗證地圖。

因為現在最大的風險是：

- 主流程是否真的穩
- 不同狀態之間是否同步
- markdown 解析與輸出是否一致

---

## 測試原則

目前測試場景應優先圍繞這三件事：

### 1. 主流程不能壞

也就是：

```text
打開
  → 記錄
  → 結束訓練
  → 保存 / 回看
```

### 2. 資料要可信

不是只看畫面有沒有顯示，而是要確認：

- 資料有沒有真的被保存
- 匯出後再匯入是否一致
- 分析是不是建立在正確資料上

### 3. 高風險轉場要特別測

例如：

- 切換動作
- 切換 variant
- rest 計時中斷
- 清除資料
- 匯入不同類型日誌

---

## 測試優先級

### P0：一定先測

- 標準訓練流程
- 訓練完成流程
- 匯出 / 再匯入一致性
- 上次紀錄提示
- rest timer 基本正確性

### P1：很重要

- variant 切換
- 自訂動作
- 月報與分析顯示
- 單位切換

### P2：之後補強

- GitHub sync
- 複雜分析比例
- 極端格式輸入

---

## P0 測試場景

### Scenario 1：標準訓練流程

目的：

- 驗證主流程可用

步驟：

```text
1. 打開工具
2. 選擇一個有預設動作的 day
3. 在至少 2 個動作下各記錄數組
4. 確認每組有顯示
5. 按下「結束訓練」
```

預期結果：

- 畫面能正常輸入
- 組數顯示正確
- `結束訓練` 後當日資料被清掉
- 日誌內容新增一筆 session

---

### Scenario 2：匯出後再匯入一致性

目的：

- 驗證資料 round-trip 是否可信

步驟：

```text
1. 完成一次訓練
2. 匯出 markdown
3. 清除目前載入內容
4. 再匯入剛剛匯出的 markdown
5. 檢查日誌內容與主要資料是否合理
```

預期結果：

- session 格式完整
- 動作名稱正確
- 組數、重量、次數存在
- 匯入後不應該出現明顯解析失敗

---

### Scenario 3：上次紀錄提示

目的：

- 驗證歷史資料是否真的能回到輸入流程

步驟：

```text
1. 先匯入含有歷史資料的 markdown
2. 打開對應 day
3. 找到之前做過的動作
4. 檢查輸入框 placeholder
5. 空白直接新增一組
```

預期結果：

- 可顯示上次重量 / 次數參考
- 空白送出時能套用最近資料
- 不應該抓到明顯錯的動作資料

---

### Scenario 4：rest timer 基本流程

目的：

- 驗證休息計時最基本邏輯

步驟：

```text
1. 在同一動作新增第 1 組
2. 等待幾秒
3. 在同一動作新增第 2 組
4. 檢查第 1 組是否顯示休息時間
```

預期結果：

- 新增第 1 組後 rest timer 啟動
- 新增第 2 組時，第 1 組寫入休息秒數
- timer 重新開始

---

### Scenario 5：結束訓練時的資料收尾

目的：

- 驗證主流程最重要的收尾點

步驟：

```text
1. 記錄幾組資料
2. 按下「結束訓練」
3. 檢查 tab、日誌、當日輸入區
```

預期結果：

- 當日 `sets` 應清空
- 該 day 被標示為完成
- 日誌中新增本次訓練
- 不應出現殘留輸入資料

---

## P1 測試場景

### Scenario 6：variant 切換

目的：

- 驗證主名稱與變體名稱映射是否穩定

步驟：

```text
1. 在有 variants 的動作中選 variant A
2. 記錄數組
3. 切換到 variant B
4. 記錄數組
5. 完成訓練並重新匯入
```

預期結果：

- 兩種變體都能正確保存
- 日誌名稱合理
- 上次紀錄與分析不應明顯錯亂

---

### Scenario 7：自訂動作

目的：

- 驗證自訂動作流程可用

步驟：

```text
1. 新增一個自訂動作
2. 記錄至少 1 組
3. 結束訓練
4. 匯出再匯入
```

預期結果：

- 自訂動作能出現在當日畫面
- 訓練完成後可進入 markdown
- 再匯入後仍能看到對應內容

---

### Scenario 8：單位切換

目的：

- 驗證 `kg / lb` 切換邏輯

步驟：

```text
1. 對某動作切到 lb
2. 輸入重量並記錄
3. 檢查顯示是否合理
4. 結束訓練後查看 markdown
```

預期結果：

- UI 顯示單位切換正常
- 換算提示合理
- markdown 應以 kg 為基準輸出

---

### Scenario 9：分析頁基本可用

目的：

- 驗證歷史匯入後主要分析區沒有明顯壞掉

步驟：

```text
1. 匯入有多次訓練紀錄的 markdown
2. 打開分析面板
3. 檢查長條圖、雷達圖、PR 區
```

預期結果：

- 面板能正常顯示
- 不應是空白或明顯錯誤
- PR 下拉可選擇動作

---

### Scenario 10：月報基本可用

目的：

- 驗證月報至少能正確顯示主要統計

步驟：

```text
1. 匯入多筆跨月份資料
2. 打開月報面板
3. 檢查月份卡片與月曆分布
```

預期結果：

- 月份統計出現
- 訓練天數合理
- 不應該整個空白或錯位

---

## 高風險轉場測試

### Scenario 11：rest 中斷後切換動作

目的：

- 驗證 rest timer 在轉場時是否正確處理

步驟：

```text
1. 某動作新增 1 組
2. 等待幾秒
3. 切到另一個動作新增 1 組
4. 回頭檢查前一動作資料
```

預期結果：

- 系統應明確決定是保存還是丟棄 rest
- 不應出現保存到錯的組

---

### Scenario 12：刪除組數後再繼續記錄

目的：

- 驗證陣列 index 與 rest 資料有沒有亂掉

步驟：

```text
1. 同一動作新增 3 組
2. 刪掉中間一組
3. 再新增一組
4. 檢查顯示與資料順序
```

預期結果：

- 組數順序應合理
- 不應出現明顯 rest 對錯組

---

### Scenario 13：清除當日資料

目的：

- 驗證中途清除是否真的乾淨

步驟：

```text
1. 在某 day 記幾組
2. 按清除當日
3. 檢查 UI 與 tab 狀態
```

預期結果：

- 當日組數清空
- 完成狀態被移除
- 不應殘留舊資料 chip

---

### Scenario 14：清除所有暫存

目的：

- 驗證 localStorage 相關暫存是否真的清乾淨

步驟：

```text
1. 先建立暫存資料與自訂動作
2. 按清除所有暫存
3. 重新整理 / 重開
```

預期結果：

- 未完成訓練資料消失
- 自訂動作消失
- finished day 狀態消失

---

## P2 測試場景

### Scenario 15：GitHub Pull / Push

目的：

- 驗證開發維護功能是否仍可用

步驟：

```text
1. 填入 GitHub 設定
2. 測試 pull
3. 測試 push
```

預期結果：

- 至少能顯示成功 / 失敗狀態
- 不應因為此功能影響主流程

---

### Scenario 16：舊格式或異常格式 markdown

目的：

- 驗證匯入穩定性與失敗表現

步驟：

```text
1. 匯入格式略不同的 markdown
2. 打開日誌 / 分析 / 月報
```

預期結果：

- 若不能完整解析，也應知道是哪裡出問題
- 不應 silent failure 到完全不知情

---

## 通過標準

目前不需要很正式的 automated pass / fail 標準，但至少應該做到：

### 主流程通過標準

- 能順利完成一次完整訓練記錄
- 結束訓練後資料能保存
- 匯出再匯入後主要內容一致

### 資料通過標準

- 上次紀錄提示合理
- rest 不明顯錯位
- 歷史解析不明顯漏資料

### 附加功能通過標準

- 分析、月報、PR 至少在常見資料下可正常顯示

---

## 這份文件目前的用途

這份文件接下來可以作為：

- 手測 checklist
- 修 bug 後回歸測試清單
- 重構前後對照依據
- 未來自動化測試的雛形

到這一步，文件系統已經有一套完整基礎：

- `CURRENT_STATE.md`：現在有什麼
- `KNOWN_ISSUES.md`：哪裡可能有問題
- `USER_FLOW.md`：現在怎麼用
- `PRODUCT_SCOPE.md`：哪些功能最重要
- `DATA_MODEL.md`：資料怎麼流
- `TEST_SCENARIOS.md`：應該怎麼驗證

下一步如果還要繼續，不一定要再補文件了，可以開始根據這套文件做：

- bug 分級
- 功能取捨
- 重構順序
