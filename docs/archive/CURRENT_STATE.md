# Current State

## 這份文件的目的

這份文件用來描述目前 `workout_logger.html` 已經存在的功能、設計結構與高風險區域。

它的用途不是規劃未來，也不是記錄 Android 路線，而是回答這個問題：

> 這個專案現在到底已經做了什麼？它是怎麼運作的？哪些地方最容易藏 bug？

## 這份文件適合什麼時候看

當你要做下面這些事情時，應該先看這份：

- 盤點目前已完成的功能
- 理解目前程式的整體結構
- 找出高風險邏輯區
- 討論哪些該先修、哪些該先保留

## 這份文件和其他文件的關係

- 上層入口： [README.md](./README.md)
- 方向規劃： [APP_TRANSITION_PLAN.md](./APP_TRANSITION_PLAN.md)
- 這份文件角色：負責「現況解碼」

---

## 一句話總結

這個專案目前不是單純的訓練記錄器，而是一個把以下能力全部塞進同一個 HTML 檔案的手機優先工具：

- 課表執行
- 每組訓練記錄
- 休息計時
- Markdown 日誌匯入匯出
- 歷史紀錄解析
- 訓練分析
- 月報
- GitHub 同步

---

## 現況定位

目前的 `workout_logger.html` 比較像：

- 一個個人使用的訓練流程工具
- 一個單檔前端 App 原型
- 一個同時包含資料輸入、資料儲存、資料解析、資料分析的混合系統

它已經不是單純的 UI prototype，因為它已經承擔真實訓練流程。

---

## 目前功能總覽

### 1. 固定課表系統

目前內建：

- `Day 1` 胸 / 三頭
- `Day 2` 背 / 二頭
- `Day 3` 腿
- `Day 4` 肩 / 後鏈
- `自由日`

每一天的資料包含：

- 顯示名稱
- 主要 / 次要肌群
- 動作清單
- 推薦組數
- 推薦 reps
- 是否為每手重量
- 可切換的動作變體

核心來源：`PLAN`

---

### 2. 每組訓練記錄

使用者可以在每個動作下記錄：

- 重量
- 次數
- 組間休息

目前支援：

- 點擊加入一組
- 顯示目前已記錄組數
- 點擊某一組刪除
- 切換 `kg / lb`
- 顯示換算提示

---

### 3. 上次訓練資料提示

系統會從已匯入的 markdown 日誌中，抓每個動作最近一次紀錄，作為本次輸入參考。

目前用途包括：

- 顯示上次同組可能使用的重量
- 顯示上次同組可能使用的次數
- 使用者空白送出時，可直接沿用最近資料

這是一個很重要的訓練流程加速功能。

---

### 4. 單一休息計時器

系統目前有一個全域 rest timer。

特性：

- 每新增一組後自動開始計時
- 同動作接續記錄下一組時，會把上一組休息秒數寫回
- 可手動停止
- 切到其他動作時，可能會丟掉原本休息資料

這是一個很實用，但也很容易出現邏輯錯誤的功能區。

---

### 5. 本地暫存

系統使用 `localStorage` 暫存：

- 當前尚未完成的訓練資料
- 完成過的 day 狀態
- 自訂動作
- GitHub 設定

這代表目前資料保存方式偏向：

- 單機
- 暫存型
- 無正式資料庫

---

### 6. Markdown 日誌匯入 / 匯出

目前訓練歷史的主要交換格式是 markdown。

使用方式：

- 匯入既有 `train_log.md`
- 在 App 中解析歷史紀錄
- 訓練完成時，把今天資料 append 成新 markdown
- 匯出成下載檔案

這是目前整個系統最核心的資料流之一。

---

### 7. 日誌面板

日誌面板目前支援：

- 顯示目前載入的 markdown
- 匯入訓練日誌
- 匯出目前日誌
- 清除已載入日誌
- 清除所有暫存訓練資料

它本質上是系統的資料入口與資料檢查視窗。

---

### 8. 訓練分析面板

目前已有的分析能力包括：

- 近 4 週肌群訓練量分布
- 時間衰減權重
- 肌群不平衡警告
- 徒手動作摘要
- 長條圖
- 雷達圖

分析依據主要來自：

- markdown 歷史紀錄
- `MUSCLE_SCORES` 的肌群配分規則

---

### 9. 力量推估與比例分析

目前會從歷史訓練資料估算各動作 1RM，並做：

- 臥推估算
- 拮抗肌群比例比較
- 同肌群內部動作比例比較
- 單位或重量比例異常提示

這一塊已經不只是「看紀錄」，而是帶有推論型分析。

---

### 10. PR 趨勢圖

目前可依動作生成：

- 歷史 PR 走勢
- 每次 session 最佳估算 1RM
- 最高值
- 最近值
- 總進步幅度
- PR 次數

圖表使用 `canvas` 手動畫。

---

### 11. 月報系統

目前月報可提供：

- 每月訓練天數
- 每月總組數
- 每月總 volume
- 月曆式訓練分布
- 近 30 天訓練次數

這也是從 markdown 歷史反向解析出來的。

---

### 12. 自訂動作系統

目前可在任一天加入自訂動作。

加入方式：

- 先選肌群再選動作
- 直接輸入動作名稱

能力包括：

- 記錄組數
- 刪除自訂動作
- 若名稱能對應到原本課表中的動作，沿用推薦組數 / reps / 每手標記

---

### 13. GitHub 同步

目前 GitHub 區塊的作用不是同步訓練資料，而是同步 `workout_logger.html` 這個檔案本身。

支援：

- 儲存 GitHub PAT / repo / branch / path
- Pull 遠端 HTML
- Push 當前頁面序列化後的 HTML

這是一個偏開發維護用途的功能，不是最終使用者功能。

---

## 目前設計結構

### 1. 單檔架構

目前整個系統都在一個檔案裡：

- HTML
- CSS
- JavaScript

這代表它很容易快速迭代，但也會有這些代價：

- 邏輯耦合高
- 很難分層維護
- 很難局部測試

---

### 2. 三層資料來源

目前主要資料可分成三層：

#### A. 靜態定義

- `PLAN`
- `MUSCLE_SCORES`
- `ANTAG`
- `INTRA`
- `MUSCLE_GROUP_MAP`

這些是規則與知識表。

#### B. 執行中狀態

- `sets`
- `finishedDays`
- `unitMap`
- `variantMap`
- `customExercises`
- `rest`

這些是使用者正在操作時的當前狀態。

#### C. 歷史資料與衍生資料

- `logMd`
- `lastSession`
- `dayStats`
- `prHistory`
- monthly stats
- analysis 結果

這些大多是從 markdown 解析或衍生出來的。

---

### 3. 手動 render 架構

目前沒有前端框架。

畫面更新主要依賴這些函式：

- `render()`
- `renderTabs()`
- `renderDayInfo()`
- `renderExercises()`
- `renderCustomExercises()`
- `renderFinish()`
- `renderAnalysis()`
- `renderMonthly()`

這種模式的好處是直接，但壞處是：

- 狀態與 UI 關聯容易散掉
- 某些地方可能 render 不完整
- 某些操作後可能漏掉同步更新

---

### 4. Markdown 驅動的資料流

目前很多能力不是直接依賴內部 state，而是依賴 markdown 再解析。

包含：

- 上次訓練資料
- day 歷史統計
- 近 4 週分析
- PR 歷史
- 月報

這代表 markdown 格式事實上就是目前系統的核心資料協定。

---

### 5. 規則表驅動分析

分析能力高度依賴硬編碼規則表。

例如：

- 某動作算到哪些肌群
- 哪些肌群之間要比較
- 哪些動作比例合理
- 哪些重量比例代表異常

這使得系統很靈活，但也很依賴：

- 動作命名一致
- 變體命名一致
- markdown 輸出格式一致

---

## 高風險區域

下面這些區域最值得優先小心，因為它們最容易藏隱性 bug。

### 1. 動作名稱映射

目前同一個動作可能有：

- 顯示名稱
- 變體名稱
- 分析名稱
- markdown 中的名稱
- 自訂動作名稱

只要任一層命名不一致，就可能造成：

- 上次紀錄抓不到
- 分析漏算
- PR 不顯示
- 月報數據異常

---

### 2. 變體動作與主名稱的關係

例如「臥推」與「槓鈴臥推 / 啞鈴臥推」之間目前是混合關係：

- UI 以主名稱顯示
- 寫入紀錄時可能用變體名稱
- 上次資料查詢時又可能 fallback 到主名稱

這種設計很容易在下面情境出錯：

- 同主類動作換變體時
- 分析只認變體不認主名稱時
- PR 下拉選單分組時

---

### 3. 休息計時器寫回時機

目前休息時間只在特定時機才會被寫回上一組。

這意味著下面情境特別危險：

- 練完一組後切到別的動作
- 還沒記下一組就結束訓練
- 手動刪除某組
- 清空 day

很可能產生：

- rest 沒存進去
- rest 存錯組
- UI 顯示與資料不同步

---

### 4. localStorage 狀態與 logMd 狀態分離

目前有兩種資料世界同時存在：

- `sets` 代表「現在正在練、尚未完成」
- `logMd` 代表「歷史日誌文字內容」

這兩者不是同一個資料模型。

因此容易出現：

- 訓練完成後 state 與歷史更新不同步
- 載入新 log 後 UI 沒完全刷新
- 清除某部分資料但另一部分仍存在

---

### 5. Markdown 解析過度重要

目前很多功能都建立在 regex 解析 markdown 上。

只要下列任何一點變動，就可能波及整體：

- 日期格式改了
- 表格欄位改了
- 重量單位顯示改了
- 某些空白字元不同
- 某次紀錄格式不完整

這種風險不是畫面 bug，而是資料協定 bug。

---

### 6. 自訂動作與分析規則的斷裂

自訂動作可以新增，但分析未必認得它。

這會造成：

- 訓練中能記錄
- 歷史中能看到
- 但分析可能完全不算

也就是「輸入系統」和「分析系統」不是完全一致的。

---

### 7. UI toggle 與 panel 狀態

目前有多個 panel：

- 日誌
- 分析
- 月報
- GitHub

彼此切換時靠手動顯示 / 隱藏與按鈕文字切換。

這種做法容易有：

- 某個 panel 忘記關
- 按鈕文字與實際狀態不一致
- 某些 render 時機漏掉

---

### 8. GitHub 同步和實際產品目標不同層

GitHub 同步目前同步的是 HTML 檔本身，不是訓練資料模型。

這代表它比較像開發工具，而不是產品功能。

風險在於：

- 會增加主程式複雜度
- 容易和未來 App 方向混在一起
- 若當成正式同步方案，會走偏

---

## 目前最核心的價值

即使程式裡有很多風險，這個專案已經有一個很重要的核心價值：

> 它已經能陪你完成真實訓練流程。

目前最值得保留的主流程是：

```text
打開工具
  → 選擇今天課表
  → 看上次資料
  → 記錄每組重量 / 次數 / 休息
  → 結束訓練
  → 匯出 / 保存紀錄
  → 回頭看歷史與分析
```

後續不論是修 bug、改功能、重構、App 化，都應該優先保住這條主流程。

---

## 這份文件目前的用途

這份文件接下來可以作為：

- 功能盤點基準
- 風險盤點基準
- 後續修 bug 的導航地圖
- 重構前的現況快照

如果未來要繼續往下整理，下一份最適合補的文件會是：

- `KNOWN_ISSUES.md`
- `USER_FLOW.md`
- `DATA_MODEL.md`

其中：

- `CURRENT_STATE.md` 負責說明現在有什麼
- `KNOWN_ISSUES.md` 負責列出已知問題
- `USER_FLOW.md` 負責說明理想與實際流程
- `DATA_MODEL.md` 負責定義資料結構與資料流
