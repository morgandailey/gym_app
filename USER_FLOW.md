# User Flow

## 這份文件的目的

這份文件用來定義這個專案目前的實際使用流程，以及之後要優化的理想流程。

它要回答的問題是：

> 使用者現在到底怎麼用這個工具？未來希望怎麼用？

## 這份文件適合什麼時候看

當你要做下面這些事情時，應該先看這份：

- 討論主流程要不要改
- 判斷哪些功能真的重要
- 設計 App 首頁或訓練流程
- 決定哪些步驟太麻煩，應該被簡化

## 這份文件和其他文件的關係

- 上層入口： [README.md](./README.md)
- 現況總覽： [CURRENT_STATE.md](./CURRENT_STATE.md)
- 問題盤點： [KNOWN_ISSUES.md](./KNOWN_ISSUES.md)
- 方向規劃： [APP_TRANSITION_PLAN.md](./APP_TRANSITION_PLAN.md)
- 這份文件角色：負責「使用流程」

---

## 一句話總結

目前的歷史使用流程是：

```text
打開 HTML
  → 在網頁上操作
  → 手動加入訓練日誌
  → 練完後按「結束訓練」
```

但接下來要轉成的新主流程應該是：

```text
打開工具
  → 直接接續本機既有訓練資料
  → 記錄本次訓練
  → 練完按「結束訓練」
  → 自動保留歷史
  → 視需要手動匯出
```

這代表目前系統的核心價值不是分析，也不是月報，而是：

> 在訓練當下，能夠順手把每組資料記下來，並在結束時產生新的訓練紀錄。

---

## 目前實際使用流程

根據目前描述，你現在的真實流程是：

### Flow A：標準訓練流程

```text
1. 打開 workout_logger.html
2. 在網頁上使用工具
3. 手動加入訓練日誌
4. 訓練過程中持續記錄
5. 練完後按「結束訓練」
```

---

## 目前流程拆解

### Step 1：打開工具

你現在是直接打開 `workout_logger.html`。

這一步的特徵是：

- 還不是正式 App 啟動方式
- 更像開啟一個本地頁面或網頁工具

### Step 2：在網頁上操作

打開後，你會在網頁介面上進行訓練操作。

這代表目前主要使用場景是：

- 手機上的瀏覽器式操作
- 不是原生 App 式操作

### Step 3：手動加入訓練日誌

目前你提到的關鍵行為是「手動加入訓練日誌」。

這代表目前流程中，資料輸入是核心動作。

這一步通常隱含：

- 選擇今天要練的內容
- 輸入每組資料
- 依照訓練過程持續補記

### Step 4：訓練中持續記錄

訓練不是一次輸入完成，而是邊練邊記。

這代表這個工具最重要的設計要求是：

- 輸入要快
- 步驟要少
- 當下不能卡

### Step 5：按「結束訓練」

這是目前流程的結尾動作。

它代表：

- 當天訓練告一段落
- 系統應該保存 / 匯出 / 完成當天紀錄

這個按鈕其實是整個主流程最關鍵的收尾點。

---

## 新的主流程決策

這是目前已經確定的方向：

- `匯入訓練紀錄` 不再是每次使用的必要步驟
- `匯出訓練紀錄` 保留
- 歷史資料應以本機延續為主
- 匯入未來只保留給特殊情境

### 匯入之後的角色

未來 `匯入` 比較適合被視為：

- 舊版資料遷移工具
- 資料救援工具
- 低頻維護功能

而不是日常訓練入口。

### 匯出之後的角色

未來 `匯出` 保留為：

- 備份
- 手動留存
- 跨版本搬移

### 主流程結論

未來日常使用流程不應該是：

```text
每次打開
  → 先匯入
  → 再開始練
```

而應該是：

```text
打開即接續
  → 直接開始練
  → 結束後自動留在本機歷史
```

---

## 目前流程的核心特徵

從這個流程來看，現在的產品核心有幾個明顯特徵：

### 1. 它是訓練當下使用的工具

不是事後補紀錄，也不是單純看報表。

### 2. 它偏向「執行流程工具」

重點不是管理很多設定，而是陪你完成今天的訓練。

### 3. 輸入體驗比分析體驗更重要

因為你真正高頻使用的是：

- 打開
- 記錄
- 結束

不是天天都在看分析圖。

### 4. `結束訓練` 是主流程收斂點

未來很多功能都應該圍繞這個動作思考：

- 是否要自動保存
- 是否要產生歷史紀錄
- 是否要回到首頁
- 是否要更新統計

---

## 目前流程的痛點方向

雖然你現在只先提供了簡化版流程，但從這條流程已經可以看出幾個值得注意的方向：

### 1. 啟動方式還不是 App 感

現在是打開 HTML，不是點 App 圖示直接進入訓練。

### 2. 「手動加入訓練日誌」可能太籠統

這一步未來需要再拆清楚：

- 是先匯入舊日誌？
- 是直接新增今天訓練？
- 是邊輸入每組，還是先建好當天項目？

### 3. 訓練中的最短操作路徑還沒被正式定義

如果之後要優化，應該要把這件事講得更精準：

- 記一組最少要點幾下
- 看上次資料需不需要切畫面
- 動作切換會不會打斷節奏

---

## 目前版本的主流程定義

先用最簡單的方式定義目前主流程：

```text
打開工具
  → 進入訓練頁
  → 手動記錄訓練內容
  → 完成訓練
  → 按下「結束訓練」
```

未來所有功能都應該優先支援這條流程，而不是破壞它。

---

## 下一版應該補充的流程資訊

這份文件目前先記錄你已確認的真實流程。

之後最值得補充的，是把下面幾件事再說清楚：

- 打開後第一眼最想看到什麼
- 你現在說的「手動加入訓練日誌」具體是哪些步驟
- 訓練中最常操作的是哪些按鈕
- 訓練結束後你期待系統自動做什麼
- 你之後回頭看紀錄的頻率高不高

---

## 理想流程草稿

目前先不下結論，只先留一個之後要優化的方向：

```text
點開 App
  → 直接進入今天訓練
  → 快速記錄每組
  → 系統自動保存本機歷史
  → 練完按「結束訓練」
  → 自動進入本次訓練摘要或歷史紀錄
  → 需要時再手動匯出
```

這部分之後可以再根據你的習慣調整。

---

## 這份文件目前的用途

這份文件現在的角色是：

- 記錄目前真實使用方式
- 幫後續產品討論提供基準
- 防止之後設計出偏離你實際使用習慣的流程

下一份最適合補的文件會是：

- `PRODUCT_SCOPE.md`

因為現在已經有：

- `CURRENT_STATE.md` 說明現在有什麼
- `KNOWN_ISSUES.md` 說明現在可能有什麼問題
- `USER_FLOW.md` 說明你現在怎麼用

接下來就該定義：

> 第一版真正要保留哪些功能，哪些先不要做。
