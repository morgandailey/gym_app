# Data Model

## 這份文件的目的

這份文件用來描述目前這個專案的資料模型、資料來源與資料流。

它要回答的問題是：

> 這個系統現在到底在管理哪些資料？哪些是核心資料？哪些只是衍生資料？資料彼此之間怎麼流動？

## 這份文件適合什麼時候看

當你要做下面這些事情時，應該先看這份：

- 修資料相關 bug
- 重構狀態管理
- 調整匯入匯出流程
- 決定未來 App 化後資料應該怎麼保存

## 這份文件和其他文件的關係

- 上層入口： [README.md](./README.md)
- 現況總覽： [CURRENT_STATE.md](./CURRENT_STATE.md)
- 問題盤點： [KNOWN_ISSUES.md](./KNOWN_ISSUES.md)
- 使用流程： [USER_FLOW.md](./USER_FLOW.md)
- 產品範圍： [PRODUCT_SCOPE.md](./PRODUCT_SCOPE.md)
- 方向規劃： [APP_TRANSITION_PLAN.md](./APP_TRANSITION_PLAN.md)
- 這份文件角色：負責「資料模型定義」

---

## 一句話總結

目前這個系統不是圍繞單一資料模型在運作，而是同時存在三層資料：

- 靜態規則資料
- 訓練中的暫存資料
- 從 markdown 解析出的歷史與衍生資料

這也是目前很多 bug 容易出現的根本原因。

---

## 整體資料地圖

```text
靜態規則
  ├─ PLAN
  ├─ MUSCLE_SCORES
  ├─ ANTAG
  ├─ INTRA
  └─ MUSCLE_GROUP_MAP

執行中狀態
  ├─ activeDay
  ├─ sets
  ├─ finishedDays
  ├─ unitMap
  ├─ variantMap
  ├─ customExercises
  └─ rest

歷史 / 匯入資料
  └─ logMd

從歷史衍生出的資料
  ├─ lastSession
  ├─ dayStats
  ├─ prHistory
  ├─ monthly stats
  └─ analysis results
```

---

## 第一層：靜態規則資料

這些資料不是使用者當下輸入的，而是程式內建的規則與定義。

### 1. `PLAN`

用途：

- 定義每個訓練日
- 定義每個訓練日有哪些動作
- 定義推薦組數、reps、變體、是否每手

角色：

- 課表主資料
- UI 顯示來源
- 輸入卡片生成來源
- 某些分析與自訂動作對應的基礎

它是目前整個訓練執行流程最重要的靜態資料。

---

### 2. `MUSCLE_SCORES`

用途：

- 把動作映射到肌群
- 定義每個動作對各肌群的分數權重

角色：

- 肌群分析的核心規則表
- 自訂動作肌群選單的基礎資料來源之一

---

### 3. `ANTAG`

用途：

- 定義拮抗肌群或力量比例的比較規則

角色：

- 力量比例分析規則

---

### 4. `INTRA`

用途：

- 定義同肌群內不同動作之間的比例檢查規則

角色：

- 進階力量 / 動作比例分析規則

---

### 5. `MUSCLE_GROUP_MAP`

用途：

- 把分析肌群名稱對應到 UI 中使用的肌群大類

角色：

- 自訂動作選單映射

---

## 第二層：執行中狀態

這一層代表使用者正在訓練時的當前資料。

### 1. `activeDay`

用途：

- 表示目前正在看的訓練日

角色：

- UI 狀態
- render 依據

---

### 2. `sets`

這是目前最核心的執行中資料。

用途：

- 暫存今天尚未完成的訓練組數資料

概念上大致像：

```text
sets = {
  "Day 1": {
    "槓鈴臥推 (Barbell Bench Press)": [
      { weight, reps, rest }
    ],
    "啞鈴側平舉": [
      { weight, reps, rest }
    ]
  }
}
```

角色：

- 當前訓練主資料
- 匯出 markdown 的來源
- UI 組數顯示來源
- `finish` 時最重要的輸入資料

---

### 3. `finishedDays`

用途：

- 標記哪些訓練日已完成

角色：

- tab 顯示狀態
- 訓練日完成標記

它是 UI 輔助狀態，不是歷史資料本體。

---

### 4. `unitMap`

用途：

- 記錄各動作目前是以 `kg` 還是 `lb` 顯示 / 輸入

角色：

- UI 偏好狀態

它不應該被當成歷史資料。

---

### 5. `variantMap`

用途：

- 記錄某個主動作目前切到哪個 variant

角色：

- UI 狀態
- 輸入動作對應選擇

這也是暫時狀態，不是歷史資料。

---

### 6. `customExercises`

用途：

- 儲存每一天額外加入的自訂動作

概念上大致像：

```text
customExercises = {
  "Day 1": ["某個自訂動作", "另一個自訂動作"]
}
```

角色：

- 自訂動作 UI 來源
- 匯出時補進訓練紀錄

---

### 7. `rest`

用途：

- 儲存目前唯一一個正在計時的休息狀態

概念上大致像：

```text
rest = {
  ex,
  day,
  setIdx,
  startTs,
  elapsed,
  terminated
}
```

角色：

- 單一全域計時器狀態

這是當前狀態中最脆弱的一塊，因為它和 `sets` 是分開存的。

---

## 第三層：歷史 / 匯入資料

### 1. `logMd`

用途：

- 保存目前載入的整份 markdown 訓練日誌

角色：

- 歷史資料來源
- 分析資料來源
- PR、月報、上次紀錄的來源
- `finish` 後 append 新紀錄的文字基底

這是一個很特別的設計：

目前歷史資料不是結構化物件，而是一整段字串。

---

## 第四層：從歷史衍生出的資料

這些資料不是手動輸入的，而是從 `logMd` 解析出來的。

### 1. `lastSession`

用途：

- 保存每個動作最近一次訓練時的組數資料

角色：

- 輸入 placeholder
- 自動帶入最近數據

它是「訓練中輔助資料」，不是主歷史資料本體。

---

### 2. `dayStats`

用途：

- 統計每個 day 練過幾次
- 記錄最後一次日期

角色：

- tab 狀態顯示
- day 標題資訊顯示

---

### 3. `prHistory`

用途：

- 保存各動作歷史 session 的最佳估算 1RM

角色：

- PR 下拉選單
- PR 趨勢圖

---

### 4. `monthly stats`

用途：

- 依月份聚合訓練次數、組數、總 volume

角色：

- 月報頁面資料來源

---

### 5. `analysis results`

用途：

- 保存近 4 週肌群 volume、不平衡判斷、雷達圖等分析結果

角色：

- 分析面板資料來源

通常這類資料應被視為衍生資料，而不是永久主資料。

---

## localStorage 中的資料

目前會進 localStorage 的資料主要有：

- `sets`
- `finishedDays`
- `customExercises`
- GitHub 設定

這代表 localStorage 目前主要承擔的是：

- 暫存狀態保存
- 使用者偏好 / 設定保存

不是正式歷史資料庫。

---

## 目前最核心的資料流

### Flow A：訓練中資料流

```text
使用者輸入
  → 更新 sets
  → 更新 rest
  → render UI
  → 暫存到 localStorage
```

這是訓練中最重要的即時資料流。

---

### Flow B：訓練完成資料流

```text
sets
  → buildEntry()
  → 轉成 markdown 字串
  → append 到 logMd
  → 匯出 markdown
  → 清除當日 sets
```

這是目前「歷史資料生成」的主流程。

---

### Flow C：匯入歷史資料流

```text
匯入 markdown 檔
  → 存入 logMd
  → parseLastSession()
  → 產生 lastSession / dayStats
  → render log / analysis / monthly / PR
```

這是目前「歷史資料回讀」的主流程。

---

## 新的資料流方向決策

目前已確定的產品方向是：

- `匯入 markdown` 不再是日常主流程
- `本機歷史資料` 應該成為主要歷史來源
- `markdown` 應逐步退回為匯出 / 備份格式

這代表未來理想資料流應該是：

### 日常使用

```text
當前訓練資料
  → 寫入本機結構化歷史
  → UI 與分析從本機歷史讀取
  → 需要時再匯出 markdown
```

### 特殊情境

```text
舊版資料或外部備份
  → 匯入
  → 轉換成目前本機歷史格式
```

也就是說，未來 `匯入` 更像 migration / restore，而不是 daily workflow。

---

## 目前資料模型的結構問題

### 1. 核心資料沒有統一主模型

目前同時有：

- `sets`
- `logMd`
- `lastSession`
- `prHistory`
- monthly stats

但這些不是從單一 canonical model 衍生出來的，而是多個世界並存。

這會造成：

- 同步困難
- 狀態來源不清楚
- 修改某一層容易漏另一層

---

### 2. 歷史資料是字串，不是結構化資料

`logMd` 雖然很方便匯入匯出，但問題是：

- 難以安全編輯
- 難以部分更新
- 很依賴解析格式完全一致

它比較像交換格式，不像內部最佳主資料格式。

---

### 3. 衍生資料太多，且多數即時計算

例如：

- `lastSession`
- `dayStats`
- `prHistory`
- `analysis`
- `monthly stats`

這些都不是原始資料，而是解析與推論的結果。

一旦原始資料有誤，衍生資料全部一起不可信。

---

### 4. UI 狀態與業務資料混在同一層

目前像這些都放在同一批全域變數中：

- `activeDay`
- `unitMap`
- `variantMap`
- `sets`
- `logMd`

但其實它們性質不同：

- 有些是 UI 狀態
- 有些是執行中業務資料
- 有些是歷史主資料

這會提高維護難度。

---

## 目前資料模型的核心結論

目前真正最重要的原始資料其實只有兩塊：

### 1. 當前訓練資料

- `sets`

### 2. 歷史訓練資料

- `logMd`

其他大多數資料不是原始資料，而是：

- UI 狀態
- 規則表
- 衍生結果

這個判斷很重要，因為之後如果要重構或 App 化，最先要穩住的就是這兩塊。

---

## 如果未來要演進，資料層理想方向

現在先不做實作，只先記方向。

未來比較理想的形態會是：

```text
結構化歷史資料
  ← 作為唯一主資料
    ├─ 當前訓練狀態
    ├─ 歷史紀錄
    ├─ 分析資料由此衍生
    └─ 匯出 markdown 只是輸出格式
```

也就是說：

- markdown 應該更像匯入匯出格式
- 不應該長期作為內部唯一歷史主模型

---

## 中長期資料擴充方向

如果未來要把健身系統擴成更大的外觀追蹤系統，資料層大致會增加三個子域：

### 1. 訓練資料

- 課表
- 每組紀錄
- 訓練歷史

### 2. 身體指標資料

- 體重
- 體脂
- 腰圍
- 週 / 月身體趨勢

### 3. 外觀實驗資料

- 體態照片
- 穿搭紀錄
- 髮型紀錄
- 眼鏡紀錄

參考 `AppearanceLab` 的結構，未來比較合理的做法不是把所有東西直接混進現有 `workout_logger.html`，而是把它們視為同一產品下的不同資料模組。

---

## 這份文件目前的用途

這份文件接下來可以作為：

- 修資料 bug 的參考地圖
- 重構狀態管理的基準
- App 化前資料層討論的起點
- 未來定義結構化資料模型的前置文件

下一份最適合補的文件會是：

- `TEST_SCENARIOS.md`

因為現在已經知道：

- 有哪些資料
- 資料怎麼流
- 哪些資料是核心
- 哪些地方最容易出錯

接下來就很適合把這些風險轉成可執行的驗證場景。
